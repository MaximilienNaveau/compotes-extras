# Local dev instance config. Run from here:
#   cd dev && nix run .. -- up -D
# (.. so it uses this checkout's flake, not a fetched github: copy - for
# active work on compotes-extras itself, not a standalone deployment; see
# ../examples/ for that.)
#
# APP_NAME is set explicitly here just to be self-documenting - it matches
# process-compose.yaml's own default. See ../.env.example for every knob.
APP_NAME=dev
